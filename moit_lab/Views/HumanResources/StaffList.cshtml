@*@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>*@
@*@model IEnumerable<Microsoft.AspNetCore.Identity.EntityFrameworkCore.IdentityDbContext>*@
@model IEnumerable<moit_lab.Models.Staff>


<h2>Roles</h2>
<table class="table">
    @foreach (var staffMember in Model)
    {
    <tr>
        <td>@staffMember.FamilyName</td>
        <td>@staffMember.Name</td>
        <td>@staffMember.Surmane</td>
        @*<td>
            <form asp-action="Delete" asp-route-id="@staffMember.Id" method="post">
                <button type="submit" class="btn btn-sm btn-danger">
                    Удалить
                </button>
            </form>
        </td>*@
    </tr>
    }
</table>
@*<a asp-action="Create">Add role</a>
    <a asp-action="UserList">List of users</a>*@